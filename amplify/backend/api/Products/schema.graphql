type file {
  bucket: String!
  region: String!
  key: String!
}

type Product @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String!
  size: Int!
  image: file
  price: Int!
}
 